version: "2.1"

extends: ./project-dependencies.yaml

dependencies:
  - project: quarkusio/quarkus
    mapping:
      dependant:
        default:
          - source: .*
            target: 1.11

  - project: kiegroup/kogito-runtimes
    dependencies:
      - project: quarkusio/quarkus
